<!DOCTYPE html>
<html>
<head>
  <title>BeerApp</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  <!--[if lt IE 9]>
  <%= javascript_include_tag "html5shiv.js" %>
  <%= javascript_include_tag "respond.min.js" %>
  <![endif]-->
</head>
<body>

  <div class="navbar-wrapper">
    <div class="container">

      <div class="navbar navbar-inverse navbar-fixed-top ">
        <div class="container">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to "BeerApp", root_path, :class =>'navbar-brand'  %>
          </div>
          <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav ">
              <li><%= link_to "Opis projektu", info_path_path  %></li>
              <li><%= link_to "Kontakt", contacts_path %></li>
            </ul>
            <% if user_signed_in? %>
            <p class="navbar-text pull-right">
              Logged in as <strong> <%= current_user.email %></strong>.
              <%= button_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn pull-right'  %>
              <% end %>
            </p>
          </div>
        </div>
      </div>

    </div>
  </div>

  <div class="container">

    <!-- Three columns of text below the carousel -->
    <div class="row text-center">
      <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
      <p class="alert alert-warning"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
    <!-- FOOTER -->

    <footer class="row">
      <p class="pull-right"><a href="#" class="wroc">Wróć na górę</a></p>
      <p>&copy; 2014 Tobiasz Kukawka &middot; Strona stworzona do zebrania danych na potrzeby pracy licencjackiej </p>
    </footer>
  </div><!-- /.container -->

  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
</body>
</html>
